---
description: Стек проекта — Laravel API и Vue SPA, без Blade и web-маршрутов
alwaysApply: true
---

# Стек

Канон narrative: `docs/Architecture/Stack.md`, `docs/Project/Roles.md`, `docs/Project/Roadmap.md`.

Кратко: Laravel 13 API (Sail) + Vue 3 SPA в `frontend/`. Sanctum Bearer. Не Blade, не `routes/web.php`. Vite на хосте (5173), API `http://localhost:8080/api/*`.

RAG + Ollama + copilot: `docs/API/RAG.md`, `docs/Features/Copilot.md`. Setup: `docs/Development/Setup.md`.
